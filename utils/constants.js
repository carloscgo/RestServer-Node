const SUCCESS_RECORDED = 'Record added successfully'
const EXISTS_EMAIL = 'The email already exists'
const INVALID_EMAIL = 'The email is invalid'
const REQUIRED_NAME = 'The name is required'
const INVALID_PASSWORD = 'The password is invalid, should is more 6 caracters'
const INVALID_ROLE = 'The role is invalid, not exists'

module.exports = {
    SUCCESS_RECORDED,
    EXISTS_EMAIL,
    INVALID_EMAIL,
    REQUIRED_NAME,
    INVALID_PASSWORD,
    INVALID_ROLE,
}